name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Authenticate with GCP
        run: |
          echo "${{ secrets.GCP_SA_KEY }}" > key.json
          gcloud auth activate-service-account --key-file=key.json
          gcloud config set project ${{ secrets.GCP_PROJECT_ID }}

      - name: Build and Push Docker Image
        run: |
          docker build -t gcr.io/${{ secrets.GCP_PROJECT_ID }}/fe:latest .
          docker tag gcr.io/${{ secrets.GCP_PROJECT_ID }}/fe:latest gcr.io/${{ secrets.GCP_PROJECT_ID }}/your-image-name:${{ github.sha }}
          docker push gcr.io/${{ secrets.GCP_PROJECT_ID }}/fe:latest
